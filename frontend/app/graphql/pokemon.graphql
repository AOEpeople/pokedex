query getPokemon {
    pokemon {
        id
        name
        type
        catched
    }
}

mutation setCatched($id: Int!, $catched: Boolean! ) {
    setCatched(catched: $catched, id: $id) {
        id
    }
}
